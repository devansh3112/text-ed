services:
  - type: web
    name: text-ed-server
    env: node
    region: ohio  # or choose your preferred region
    buildCommand: npm install
    startCommand: node server.cjs
    envVars:
      - key: NODE_VERSION
        value: 20.x
      - key: NODE_ENV
        value: production
    plan: free 